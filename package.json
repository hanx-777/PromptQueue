{
  "name": "promptqueue-extension",
  "version": "1.0.1",
  "private": true,
  "description": "Queue prompts, reuse workflows, and steer your next message across major AI chat pages.",
  "repository": {
    "type": "git",
    "url": "https://github.com/hanx-777/PromptQueue.git"
  },
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite build --watch",
    "test": "node -e \"const fs=require('node:fs');fs.rmSync('.tmp/tests',{recursive:true,force:true});fs.mkdirSync('.tmp/tests',{recursive:true});fs.writeFileSync('.tmp/tests/package.json','{\\\"type\\\":\\\"commonjs\\\"}\\n')\" && tsc --module CommonJS --moduleResolution Node --target ES2020 --types node,chrome --skipLibCheck --esModuleInterop --jsx react-jsx --outDir .tmp/tests tests/*.test.ts && node --test .tmp/tests/tests/*.test.js",
    "check": "npm run test && npm run build",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.9.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.0.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vite": "^8.0.13"
  }
}
